'use strict';

const mocha = require('mocha');
const expect = require('chai').expect;
const google = require('../../../lib/providers/google/google-file.js');

describe('Google File Provider', ()=> {
	it.skip('retrieves GPX file from a Google Drive folder', ()=> {

	});

	it.skip('converts a GPX file to GeoJSON', ()=> {

	});

	it.skip('retrieves PDF file from a Google Drive folder', ()=> {

	});

	it.skip('writes generated PDF to folder', ()=> {

	});
});