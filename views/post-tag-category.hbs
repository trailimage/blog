{{#contentFor "head"}}
	{{> json-ld}}
{{/contentFor}}

<div class="post-tag-category content">
	{{{iconForPostTag title}}}
	<ul>
	{{#each tags}}
		<li><a href="/{{slug}}" rel="tag">{{title}}</a>({{posts.length}} post{{plural posts.length}})</li>
	{{/each}}
	</ul>
</div>